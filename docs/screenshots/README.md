# Screenshots

Replace these placeholders with real captures (PNG/JPG) after running the app:

| File | Suggested content |
|------|-------------------|
| `home.png` | Home page KPI cards + sidebar |
| `test-execution.png` | Pass/fail trend + run table |
| `defect-metrics.png` | Severity donut + burn-up |
| `trends.png` | Heatmap + Pareto + correlation |

Keep filenames exactly as above so the README image links work.
